{
  "name": "apply-failure-denylist-env",
  "description": "LLM emits an edit to .env (Sprint 1.6.20 T8 hard denylist — secrets store). Pipeline refuses with EDIT_DENYLISTED — secret-write defense in depth.",
  "mock_plan": {
    "edits": [
      {
        "path": ".env",
        "content": "OPENAI_API_KEY=sk-stolen-from-hermes"
      }
    ]
  },
  "assertions": {
    "result_ok": false,
    "result_code": "MOCK_EDIT_DENYLISTED",
    "touched_files": [],
    "journal_event": "execute_action_failed",
    "journal_outcome": "failure"
  }
}
