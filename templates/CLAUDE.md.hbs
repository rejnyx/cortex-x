# {{projectName}}

{{description}}

---

## 🔒 RULE 1 — Inviolable architectural invariants

> **Contracts, not aspirations.** Every commit in this project must respect them. Violations block merge.

### SSOT (Single Source of Truth)
Every piece of knowledge has **exactly one source of truth**. Constants → `config/`. Design tokens → `config/design-tokens.ts`. DB schema → migrations (types generated, not hand-written). Institutional wisdom → `~/.claude/shared/standards/` (stable rules) + `{{cortex_source}}/projects/` (cross-project memory). Current state → this CLAUDE.md. **Zero overlap.**

### Modular (Isolated subsystems)
Features live in `src/features/<slug>/` (or equivalent). External SDKs only via adapters. LEGO tools — one file per tool. No circular imports. Test boundary = module boundary.

### Scalable (10x-safe patterns)
RLS from day 1. Indexes on FK and query predicates. Pagination on all list endpoints. Rate limits + cost guards on external boundaries. Streaming > polling where it makes sense.

**Full contract:** `~/.claude/shared/standards/RULE-1.md`. Anti-patterns there are automatic blockers — not up for debate.

### Rule 2 (Critical): [`security.md`](../.claude/shared/standards/security.md) · [`testing.md`](../.claude/shared/standards/testing.md) · [`observability.md`](../.claude/shared/standards/observability.md) · [`correctness.md`](../.claude/shared/standards/correctness.md)

- **Security** — 8-layer defense + Agentic Security (lethal trifecta, 7 MUST patterns for LLM/agent systems)
- **Testing** — layered pyramid, 5 pillars per test file (happy / error / edge / security / integration)
- **Observability** — structured logs + burn-rate SLOs + circuit breakers + LLM obs stack
- **Correctness** — Zod at boundaries, property-based tests, eval suites for LLM, mutation testing, stateful simulation

### Rule 3 (Process): Performance · A11y · Error · Git · Docs · `~/.claude/shared/standards/ai-patterns.md`

---

## Tech Stack

- **Framework:** {{stack.framework}} + {{stack.language}} ({{#if stack.typescript_strict}}strict{{/if}})
{{#if stack.styling}}
- **Styling:** {{#each stack.styling}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}
{{#if stack.database.provider}}
- **Database:** {{stack.database.provider}} ({{#each stack.database.features}}{{this}}{{#unless @last}}, {{/unless}}{{/each}})
{{/if}}
{{#if stack.ai.provider}}
- **AI/LLM:** {{stack.ai.provider}} ({{#each stack.ai.models}}{{this}}{{#unless @last}}, {{/unless}}{{/each}})
- **AI SDK:** {{stack.ai.sdk}}
{{/if}}
{{#if stack.deploy}}
- **Deploy:** {{stack.deploy}}
{{/if}}
{{#if stack.testing}}
- **Tests:** {{stack.testing.unit}}{{#if stack.testing.e2e}} + {{stack.testing.e2e}}{{/if}}
{{/if}}

## Architecture

{{#if architecture}}
{{architecture}}
{{else}}
TODO: Architecture description — how it works end-to-end. Which modules, which boundaries, where data flows.
{{/if}}

## Conventions

- {{#if conventions.typescript_strict}}TypeScript strict mode{{/if}}
- {{#if conventions.language_ui}}UI language: {{conventions.language_ui}}{{/if}}
- {{#if conventions.language_code}}Code: {{conventions.language_code}} (variables, comments){{/if}}
{{#if conventions.component_library}}
- Components: {{conventions.component_library}}
{{/if}}
{{#if conventions.color_system}}
- Colors: {{conventions.color_system}}
{{/if}}
{{#if conventions.form_validation}}
- Validation: {{conventions.form_validation}}
{{/if}}

**Verification discipline** — pair every implementation todo with a verification todo. For UI: build → screenshot → assert (Chrome DevTools MCP). For handlers: build → run the test, read the log. Verification todo must be checked off before commit, not just the implementation one. Detail: `~/.claude/shared/standards/verification-loop.md`.

## Corrections log

Append-only record. Every time Claude makes a mistake the operator catches, capture it here so the next session reads the fix before repeating the bug.

```
- 2026-XX-XX — Claude attempted X. Operator corrected: use Y instead because Z. Root cause: missing constraint Z in section "<section name>".
```

> Pattern: **mistake → discipline → CLAUDE.md update** (Anthropic team standard practice). Five-minute updates compound across sessions.

## Development

```bash
npm install
npm run dev
npm run build
npm test
```

## Environment Variables

```env
# TODO: fill in per your stack
```

## Directory Structure

```
{{#each structure}}
{{this}}
{{/each}}
```

## Agents (review pipeline)

{{#each agents}}- `{{this}}`
{{/each}}

Invoke via `~/.claude/shared/prompts/code-review.md` — spawn in parallel with diff context. `ssot-enforcer` is always MANDATORY (Rule 1 gate).

## Hooks

Automatically active from `~/.claude/shared/hooks/`:
- `block-destructive` — rm -r, force push, hard reset, DROP TABLE
- `session-start` — sprint state + git state + Rule 1 violations in insights/
- `pre-compact` — recovery state before context compact
- `post-tool-use` — journal traces for cortex-evolve (metadata only)

## Voice & identity

Claude is the actor; cortex is the environment. One voice across `/start`, `/audit`, `/designer`, `/sync`, `/cortex-help`, Steward — no greetings, no emoji, no emotion words, no persona drift. Memory recall ALWAYS cited via `[cortex/recall]` + `[^cN]` footnote with 3-hop trace (project → file → line). When search comes up empty, declare it: `[cortex/no-recall] No prior entries for "<query>" in $CORTEX_DATA_HOME — answering from first principles.`

**Charter:** `~/.claude/shared/standards/voice.md` (full rules + 5 failure-mode templates).
**First-run sequence:** `~/.claude/shared/prompts/onboarding-first-10min.md`.

## Web research (default behavior)

cortex defaults to web-researching whenever a task depends on the **current state of an external system** — framework versions, library APIs, CVEs, regulatory thresholds, design trends, a11y standards. You don't have to say "go look it up"; Claude is expected to dispatch a parallel research subagent first, cache the result under `$CORTEX_DATA_HOME/research/`, and cite URLs.

**When research fires automatically:**
- Sprint kickoff / new feature with external dependency
- Dep upgrade with breaking-change risk
- "Best practice for X" / "modern way to" questions
- Anything where Claude's training cutoff (Jan 2026) is older than the relevant artifact

**Override:** add `--no-research` to the prompt or set `CORTEX_OFFLINE=1` to skip. Use `--force-research` to bypass cache.

**SSOT:** `~/.claude/shared/research-protocol.md` (when, how, budget caps, output discipline) + `~/.claude/shared/standards/web-research.md` (Rule 3 standard).

## Cross-project context

Institutional wisdom + cross-project patterns: `{{cortex_source}}/projects/` (live source). Load cheat sheet: `~/.claude/shared/prompts/cortex-load.md`.

---

Generated by **cortex-x** on {{date}}. Rule 1 contract: `~/.claude/shared/standards/RULE-1.md`.
