# {{projectName}}

{{description}}

---

## 🔒 RULE 1 — Inviolable architectural invariants

> **Contracts, ne aspirace.** Každý commit v tomto projektu musí respektovat. Violations blokují merge.

### SSOT (Single Source of Truth)
Každý kus znalosti má **přesně jeden zdroj pravdy**. Constants → `config/`. Design tokens → `config/design-tokens.ts`. DB schema → migrace (types generované, ne psané ručně). Institutional wisdom → `~/.claude/shared/standards/` (stable rules) + `{{cortex_source}}/projects/` (cross-project memory). Current state → tento CLAUDE.md. **Nula overlap.**

### Modular (Isolated subsystems)
Features žijí v `src/features/<slug>/` (nebo ekvivalentu). Externí SDK jen přes adapter. LEGO tools — jeden soubor per tool. Žádné kruhové importy. Test boundary = module boundary.

### Scalable (10x-safe patterns)
RLS od day 1. Indexy na FK a query predikátech. Paginace na všech listech. Rate limit + cost guards na externích boundaries. Streaming > polling kde dává smysl.

**Full contract:** `~/.claude/shared/standards/RULE-1.md`. Anti-patterns tam jsou automatic blockers — ne debate.

### Rule 2 (Critical): [`security.md`](../.claude/shared/standards/security.md) · [`testing.md`](../.claude/shared/standards/testing.md) · [`observability.md`](../.claude/shared/standards/observability.md) · [`correctness.md`](../.claude/shared/standards/correctness.md)

- **Security** — 8-layer defense + Agentic Security (lethal trifecta, 7 MUST patterns for LLM/agent systems)
- **Testing** — layered pyramid, 5 pillars per test file (happy / error / edge / security / integration)
- **Observability** — structured logs + burn-rate SLOs + circuit breakers + LLM obs stack
- **Correctness** — Zod at boundaries, property-based tests, eval suites for LLM, mutation testing, stateful simulation

### Rule 3 (Process): Performance · A11y · Error · Git · Docs · `~/.claude/shared/standards/ai-patterns.md`

---

## Tech Stack

- **Framework:** {{stack.framework}} + {{stack.language}} ({{#if stack.typescript_strict}}strict{{/if}})
{{#if stack.styling}}
- **Styling:** {{#each stack.styling}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}
{{#if stack.database.provider}}
- **Database:** {{stack.database.provider}} ({{#each stack.database.features}}{{this}}{{#unless @last}}, {{/unless}}{{/each}})
{{/if}}
{{#if stack.ai.provider}}
- **AI/LLM:** {{stack.ai.provider}} ({{#each stack.ai.models}}{{this}}{{#unless @last}}, {{/unless}}{{/each}})
- **AI SDK:** {{stack.ai.sdk}}
{{/if}}
{{#if stack.deploy}}
- **Deploy:** {{stack.deploy}}
{{/if}}
{{#if stack.testing}}
- **Tests:** {{stack.testing.unit}}{{#if stack.testing.e2e}} + {{stack.testing.e2e}}{{/if}}
{{/if}}

## Architektura

{{#if architecture}}
{{architecture}}
{{else}}
TODO: Popis architektury — jak to funguje end-to-end. Jaké moduly, jaké hranice, kam teče data flow.
{{/if}}

## Konvence

- {{#if conventions.typescript_strict}}TypeScript strict mode{{/if}}
- {{#if conventions.language_ui}}UI jazyk: {{conventions.language_ui}}{{/if}}
- {{#if conventions.language_code}}Kód: {{conventions.language_code}} (variables, comments){{/if}}
{{#if conventions.component_library}}
- Komponenty: {{conventions.component_library}}
{{/if}}
{{#if conventions.color_system}}
- Barvy: {{conventions.color_system}}
{{/if}}
{{#if conventions.form_validation}}
- Validace: {{conventions.form_validation}}
{{/if}}

## Development

```bash
npm install
npm run dev
npm run build
npm test
```

## Environment Variables

```env
# TODO: Doplnit podle stacku
```

## Directory Structure

```
{{#each structure}}
{{this}}
{{/each}}
```

## Agents (review pipeline)

{{#each agents}}- `{{this}}`
{{/each}}

Invoke via `~/.claude/shared/prompts/code-review.md` — spawn paralelně s diff context. `ssot-enforcer` je vždy MANDATORY (Rule 1 gate).

## Hooks

Automaticky aktivní z `~/.claude/shared/hooks/`:
- `block-destructive` — rm -r, force push, hard reset, DROP TABLE
- `session-start` — sprint state + git state + Rule 1 violations v insights/
- `pre-compact` — recovery state před kontextem compact
- `post-tool-use` — journal traces pro cortex-evolve (metadata only)

## Cross-project context

Institutional wisdom + cross-project patterns: `{{cortex_source}}/projects/` (live source). Load cheat sheet: `~/.claude/shared/prompts/cortex-load.md`.

---

Generated by **cortex-x** on {{date}}. Rule 1 contract: `~/.claude/shared/standards/RULE-1.md`.
